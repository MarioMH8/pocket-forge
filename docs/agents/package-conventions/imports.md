# Imports

Always use absolute `@pocket-forge/*` imports. Never use relative imports (`../domain`).

```typescript
// ✅ Correct

// ❌ Wrong
```

## Import conventions for decorators and handlers

```typescript
// Decorator: no suffix

// Handler interface: prefixed with "Interface"
```
