# Cross-platform through a web runtime

Pocket Forge has one web runtime: Phaser renders the 2D world and React renders accessible menus and application UI. Games target web and PWA directly, while Tauri and Capacitor package that same runtime for desktop and mobile. Platform-specific capabilities are optional adapters rather than dependencies of the simulation domain.
